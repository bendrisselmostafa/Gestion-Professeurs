{% extends 'base.html.twig' %}

{% block title %}Departement{% endblock %}

{% block body %}
    <h1>Departements</h1>

    <table class="table" border="1">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ departement.id }}</td>
            </tr>
            <tr>
                <th>Nom_depart</th>
                <td>{{ departement.nomDepart }}</td>
            </tr>
        <tr>
            <th>Liste Prof</th>
            <td>
                <div class="form-group">
                    <select class="custom-select">
                        <option selected="">Voir plus</option>
                {% for prof in departement.professeur %}
                            <option >{{ prof.nom }}</option>
                {% endfor %}
                </select>
                </div>
            </td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('departement_index') }}">Retour à la liste</a>

    <a href="{{ path('departement_edit', {'id': departement.id}) }}">Modifier</a>

    {{ include('departement/_delete_form.html.twig') }}
{% endblock %}
